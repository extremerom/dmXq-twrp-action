name: build OrangeFox

on:
  schedule:
  - cron: "0 21 * * 1-5"  # 04:00 AM Thailand/Vietnam (UTC+7)
  - cron: "0 9 * * 1-5"   # 16:00 PM Thailand/Vietnam (UTC+7)
  workflow_dispatch:
  
jobs:
  build:
    runs-on: depot-ubuntu-24.04-32

    environment:
      name: /dev/null
      url: /dev/null
    
    permissions:
      contents: write
      actions: write
      id-token: write

#    concurrency:
#      group: "none"
#      cancel-in-progress: false
    
    strategy:
      fail-fast: false      
      matrix:
        target: [dm2q]
#      max-parallel: 1

    steps:
    - name: Check out
      uses: actions/checkout@v4

    - name: Set TimeZones
      run: |
        sudo timedatectl set-timezone Asia/Ho_Chi_Minh
        sudo dpkg-reconfigure tzdata
        echo "date=$(TZ=Asia/Ho_Chi_Minh date +%Y-%m-%d-%H:%M:%S)"
    
    - name: Prepare the environment
      run: |
        sudo apt update
        sudo apt -y upgrade
        sudo apt -y install ccache gperf gcc-multilib gcc-10-multilib g++-multilib g++-10-multilib libc6-dev x11proto-core-dev libx11-dev tree lib32z-dev libgl1-mesa-dev libxml2-utils xsltproc bc ccache lib32readline-dev lib32z1-dev liblz4-tool libncurses5-dev libsdl1.2-dev lz4 tar libxml2 lzop pngcrush schedtool squashfs-tools imagemagick libbz2-dev lzma ncftp qemu-user-static libstdc++-10-dev python3
      continue-on-error: true
        
    - name: Set Swap Space
      uses: pierotofy/set-swap-space@master
      with:
        swap-size-gb: 1

    - name: Install openjdk
      uses: actions/setup-java@v3
      with:
        distribution: 'zulu'
        java-version: '11'    

    - name: Install repo
      run: |
        mkdir ~/bin
        curl https://storage.googleapis.com/git-repo-downloads/repo > ~/bin/repo
        chmod a+x ~/bin/repo
        sudo ln -sf ~/bin/repo /usr/bin/repo

    - name: Init repo
      run: |
        export HOME=${GITHUB_WORKSPACE}
        cd ${GITHUB_WORKSPACE}
        chmod +x *
        git config --global user.name "github-actions[bot]"
        git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
        mkdir ${GITHUB_WORKSPACE}/OrangeFox_sync
        cd ${GITHUB_WORKSPACE}/OrangeFox_sync
        git clone https://gitlab.com/OrangeFox/sync.git # (or, using ssh, "git clone git@gitlab.com:OrangeFox/sync.git")
        cd ${GITHUB_WORKSPACE}/OrangeFox_sync/sync/
        ./orangefox_sync.sh --branch 12.1 --path  ${GITHUB_WORKSPACE}/fox_12.1
        

    - name: Clone device tree
      run: |
        cd ${GITHUB_WORKSPACE}/fox_12.1
        git clone -b OrangeFox https://github.com/EduardoA3677/android_device_samsung_${{ matrix.target }}.git device/samsung/${{ matrix.target }} > /dev/null 2>&1
        
    - name: Clone common tree
      run: |
        cd ${GITHUB_WORKSPACE}/fox_12.1
        git clone -b android-12.1 https://github.com/EduardoA3677/android_device_samsung_sm8550-common.git device/samsung/sm8550-common > /dev/null 2>&1
    
    - name: Setup ccache before build
      run: |
        cd ${GITHUB_WORKSPACE}/fox_12.1
        export USE_CCACHE=1
        export CCACHE_EXEC=/usr/bin/ccache
        ccache --max-size=50G
        ccache -o compression=true
        export CC="ccache gcc"
        export CXX="ccache g++"
        ccache -s
    
    - name: Building recovery
      id: twrp
      run: |
        cd ${GITHUB_WORKSPACE}
        chmod +x build-ofox.sh
        cp build-ofox.sh  ${GITHUB_WORKSPACE}/fox_12.1/build-ofox.sh
        cd ${GITHUB_WORKSPACE}/fox_12.1
        ./build-ofox.sh ${{ matrix.target }} 1
      continue-on-error: true

    - name: Check ccache after build
      run: |
        ccache -s

    - name: Set variables
      run: |
        echo "date=$(TZ=Asia/Ho_Chi_Minh date +%Y-%m-%d-%H:%M:%S)" >> $GITHUB_OUTPUT
        echo "Outdir=${GITHUB_WORKSPACE}/fox_12.1" >> $GITHUB_OUTPUT
      id: var

    - name: Publish to github
      uses: softprops/action-gh-release@v1
      with:
        files: ${{ steps.var.outputs.Outdir }}/OrangeFox_R12.1-${{ matrix.target }}.img.tar
        name: Unofficial Release
        tag_name: ${{ github.run_id }}
        body: |
          - ${{ steps.var.outputs.date }}
      env:
        GITHUB_TOKEN: ${{ secrets.TEST }}
